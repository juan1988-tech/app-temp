import { createContext} from "react";

export const BurguerContext = createContext(null);
export const ToggleBurguerContext = createContext(null);
export const handleComponentContext = createContext(null)
export const RefContext = createContext(null);
export const SecondRefContext = createContext(null);
export const ThirdRefContext = createContext(null);
export const FourthRefContext = createContext(null);